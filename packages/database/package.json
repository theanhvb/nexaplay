{
  "name": "@movie-platform/database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./catalog": "./src/catalog.ts"
  },
  "scripts": {
    "generate:catalog": "prisma generate --schema prisma/catalog/schema.prisma",
    "validate:catalog": "prisma validate --schema prisma/catalog/schema.prisma",
    "migrate:catalog": "prisma migrate deploy --schema prisma/catalog/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  }
}
