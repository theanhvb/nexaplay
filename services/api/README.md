# API Service

Local-first Phase 1 API implementing the initial Auth, User, Catalog, Search, and Admin routes from `05_API_CONTRACTS.md`.

```bash
npm run dev --workspace services/api
```

Health check: `GET /health`
